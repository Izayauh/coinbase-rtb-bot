[tool.pytest.ini_options]
# bot/tests first (existing live-bot coverage is unchanged). research suites are
# additive — see docs/research_pipeline/ARCHITECTURE_REVIEW.md finding F-H1.
testpaths = ["bot/tests", "research/tests", "research_pipeline/tests"]
